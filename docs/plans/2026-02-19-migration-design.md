# EasyCCSwitch → VibeLever 迁移重构设计文档

> **日期**：2026-02-19
> **状态**：已通过设计评审
> **目标**：将 easy-cc-switch（Electron）全面重新设计并迁移至 vibe-lever（Tauri v2）

---

## 1. 项目背景

EasyCCSwitch 是一个基于 Electron + React + MUI 的 AI 编码工具供应商快速切换管理器，支持 Claude Code 和 Codex。本次迁移不是简单的 1:1 复制，而是一次 **完整的架构重构与功能优化**，采用 Tauri v2 作为新的桌面框架。

### 原版核心功能

- Claude Code / Codex 供应商配置管理（CRUD + 一键激活）
- 工具安装 / 卸载 / 状态检测 / 进程管理
- 全局 MCP 服务器管理（stdio / http / sse）
- MCP 市场（浏览和安装）
- CLAUDE.md 文档编辑
- Claude 项目配置管理（.claude.json）
- 深色 / 浅色主题切换
- 中英文国际化（i18n）

---

## 2. 设计决策总结

| 决策项 | 选择 |
|--------|------|
| 功能模块 | 全部 **重新设计** |
| UI 框架 | shadcn/ui + Tailwind CSS |
| 信息架构 | 按功能分类 + 工具选择器动态切换 |
| 视觉风格 | Linear / Vercel Dashboard 极简风格，浅色为主 + 深色模式 |
| 数据存储 | SQLite（rusqlite） |
| 国际化 | 中文 + 英文（i18next） |
| 窗口样式 | macOS 透明标题栏 / Windows 自定义标题栏 |
| 路由 | TanStack Router |
| 状态管理 | Zustand |
| 图标 | FontAwesome |
| 后端架构 | 重 Rust 后端（方案 A） |
| 功能范围 | 核心优先，预留扩展性 |

---

## 3. 系统架构

### 3.1 分层架构图

```
┌─────────────────────────────────────────────────────┐
│                   Frontend (React 19)                │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │ shadcn/ui│  │ Tailwind │  │ TanStack Router  │   │
│  └──────────┘  └──────────┘  └──────────────────┘   │
│  ┌──────────────────────────────────────────────┐   │
│  │            State Management (Zustand)         │   │
│  └──────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────┐   │
│  │          i18n (react-i18next / i18next)        │   │
│  └──────────────────────────────────────────────┘   │
└────────────────────┬────────────────────────────────┘
                     │ invoke() / Tauri Commands
┌────────────────────▼────────────────────────────────┐
│                Rust Backend (Tauri v2)                │
│  ┌─────────────┐  ┌─────────────┐  ┌────────────┐  │
│  │ vendor_mgr  │  │  tool_mgr   │  │  mcp_mgr   │  │
│  └─────────────┘  └─────────────┘  └────────────┘  │
│  ┌─────────────┐  ┌─────────────┐  ┌────────────┐  │
│  │ config_mgr  │  │  docs_mgr   │  │ settings   │  │
│  └─────────────┘  └─────────────┘  └────────────┘  │
│  ┌──────────────────────────────────────────────┐   │
│  │              SQLite (rusqlite)                 │   │
│  └──────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────┐   │
│  │     System Layer (fs, process, shell, toml)    │   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

### 3.2 职责划分

**前端职责**：
- UI 渲染和用户交互
- 表单展示和客户端校验
- 全局状态管理（Zustand）
- 路由与页面切换
- i18n 语言切换

**Rust 后端职责**：
- 所有文件系统操作（settings.json、config.toml、auth.json、.claude.json、CLAUDE.md）
- 所有 Shell 命令执行（工具安装 / 卸载 / 检测 / 进程管理）
- 配置文件解析与验证（JSON、TOML）
- SQLite 数据库操作
- 平台检测与适配逻辑

**前后端通信**：
- Tauri v2 `invoke()` 异步机制
- Rust 端返回 `Result<T, String>`，前端统一 catch 处理
- TypeScript 类型定义确保类型安全

---

## 4. 动态导航架构

### 4.1 核心理念

顶部工具选择器 + 数据驱动的动态侧边栏。用户选择不同工具后，侧边栏动态显示该工具的可用功能。

```
┌──────────────────────────────────────────────────────┐
│ 🔧 VibeLever                           — □ ✕       │
├──────────┬───────────────────────────────────────────┤
│          │                                           │
│ ┌──────┐ │                                           │
│ │☁ CC ▾│ │         (当前页面内容区)                   │
│ └──────┘ │                                           │
│          │                                           │
│ 📦 供应商 │                                           │
│ 🛠 工具   │                                           │
│ 🔌 MCP   │                                           │
│  ├ 服务器 │                                           │
│  └ 市场  │                                           │
│ 📄 文档   │                                           │
│  ├ CLAUDE │                                           │
│  └ 项目  │                                           │
│ ─────── │                                           │
│ ⚙️ 设置  │                                           │
├──────────┴───────────────────────────────────────────┤
│ v0.1.0                              🌐 中/EN        │
└──────────────────────────────────────────────────────┘
```

### 4.2 各工具功能对照

| 菜单项 | Claude Code | Codex | Gemini（未来） |
|--------|:-----------:|:-----:|:-------------:|
| 供应商管理 | ✅ | ✅ | ✅ |
| 工具管理 | ✅ | ✅ | ✅ |
| MCP 服务器 | ✅ | ❌ | ❓ |
| MCP 市场 | ✅ | ❌ | ❓ |
| CLAUDE.md | ✅ | ❌ | ❌ |
| 项目配置 | ✅ | ❌ | ❓ |
| 设置 | ✅（常驻） | ✅（常驻） | ✅（常驻） |

### 4.3 数据驱动菜单系统

```typescript
interface ToolDefinition {
  id: string           // 'claude-code' | 'codex' | 'gemini'
  name: string
  icon: IconDefinition
  features: Feature[]
}

interface Feature {
  id: string
  label: string        // i18n key
  icon: IconDefinition
  route: string
  children?: Feature[]
}
```

### 4.4 路由表

| 路由路径 | 页面 |
|----------|------|
| `/` | → 重定向到上次选择的工具 |
| `/$tool/vendors` | 供应商管理 |
| `/$tool/tools` | 工具安装管理 |
| `/claude-code/mcp/servers` | MCP 服务器管理 |
| `/claude-code/mcp/marketplace` | MCP 市场 |
| `/claude-code/docs/claude-md` | CLAUDE.md 编辑 |
| `/claude-code/docs/projects` | 项目配置管理 |
| `/settings` | 设置 |

---

## 5. Rust 后端模块设计

### 5.1 目录结构

```
src-tauri/src/
├── lib.rs                  # Tauri 入口，注册所有 commands
├── db/
│   ├── mod.rs              # 数据库初始化、迁移
│   └── schema.rs           # 表结构定义
├── commands/               # Tauri Commands（前端调用入口）
│   ├── mod.rs
│   ├── vendor.rs
│   ├── tool.rs
│   ├── mcp.rs
│   ├── docs.rs
│   ├── project.rs
│   ├── settings.rs
│   └── system.rs
├── services/               # 业务逻辑层
│   ├── mod.rs
│   ├── vendor_service.rs
│   ├── tool_service.rs
│   ├── mcp_service.rs
│   ├── docs_service.rs
│   ├── project_service.rs
│   └── settings_service.rs
├── models/                 # 数据模型
│   ├── mod.rs
│   ├── vendor.rs
│   ├── tool.rs
│   ├── mcp.rs
│   └── settings.rs
└── utils/                  # 工具函数
    ├── mod.rs
    ├── platform.rs
    ├── shell.rs
    └── config_parser.rs
```

### 5.2 核心 Tauri Commands

**供应商管理**：
- `get_vendors(tool)` → `Vec<Vendor>`
- `add_vendor(tool, vendor)` → `Vendor`
- `update_vendor(id, vendor)` → `Vendor`
- `delete_vendor(id)` → `()`
- `activate_vendor(tool, id)` → `()` （同时写入对应配置文件）

**工具管理**：
- `check_tool_status(tool)` → `ToolStatus`
- `install_tool(tool, method)` → `InstallResult`
- `uninstall_tool(tool)` → `()`
- `check_tool_running(tool)` → `bool`
- `kill_tool_process(tool)` → `()`

**MCP 管理**：
- `get_mcp_servers()` → `Vec<McpServer>`
- `add_mcp_server(server)` → `McpServer`
- `update_mcp_server(name, server)` → `()`
- `delete_mcp_server(name)` → `()`

**文档管理**：
- `get_claude_md(path?)` → `Option<String>`
- `save_claude_md(path?, content)` → `()`

**系统与设置**：
- `get_platform_info()` → `PlatformInfo`
- `get_settings()` → `AppSettings`
- `update_settings(settings)` → `()`

### 5.3 SQLite 表结构

```sql
-- 供应商配置表（所有工具共用）
CREATE TABLE vendors (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    tool        TEXT NOT NULL,           -- 'claude-code' | 'codex' | 'gemini'
    name        TEXT NOT NULL,
    vendor_key  TEXT,                    -- 预设供应商标识
    base_url    TEXT NOT NULL,
    token       TEXT NOT NULL,
    model       TEXT,
    config_json TEXT,                    -- 工具特有扩展配置（JSON）
    is_active   BOOLEAN DEFAULT 0,
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 用户设置表
CREATE TABLE settings (
    key   TEXT PRIMARY KEY,
    value TEXT NOT NULL
);
```

---

## 6. 前端组件架构

### 6.1 目录结构

```
src/
├── components/                 # 通用组件（目录 + index.tsx）
│   ├── sidebar/
│   │   └── index.tsx
│   ├── title-bar/
│   │   └── index.tsx
│   ├── vendor-card/
│   │   └── index.tsx
│   ├── tool-status-card/
│   │   └── index.tsx
│   ├── mcp-server-card/
│   │   └── index.tsx
│   ├── vendor-form-dialog/
│   │   └── index.tsx
│   ├── mcp-form-dialog/
│   │   └── index.tsx
│   ├── confirm-dialog/
│   │   └── index.tsx
│   ├── empty-state/
│   │   └── index.tsx
│   └── loading-skeleton/
│       └── index.tsx
├── pages/                      # 页面组件（目录 + index.tsx）
│   ├── vendors/
│   │   └── index.tsx
│   ├── tools/
│   │   └── index.tsx
│   ├── mcp-servers/
│   │   └── index.tsx
│   ├── mcp-marketplace/
│   │   └── index.tsx
│   ├── claude-md/
│   │   └── index.tsx
│   ├── projects/
│   │   └── index.tsx
│   └── settings/
│       └── index.tsx
├── routes/                     # TanStack Router 路由文件
├── stores/                     # Zustand 状态管理
│   ├── use-tool-store.ts
│   ├── use-vendor-store.ts
│   ├── use-settings-store.ts
│   └── use-platform-store.ts
├── hooks/                      # 自定义 Hooks
│   ├── use-tauri-command.ts
│   └── use-platform.ts
├── lib/                        # 工具函数
│   ├── i18n.ts
│   ├── tauri.ts
│   └── utils.ts
├── types/                      # TypeScript 类型定义
│   ├── vendor.ts
│   ├── tool.ts
│   ├── mcp.ts
│   └── settings.ts
└── styles/
    └── globals.css
```

### 6.2 状态管理

```typescript
// use-tool-store.ts — 当前工具选择
interface ToolStore {
  currentTool: 'claude-code' | 'codex' | 'gemini'
  setCurrentTool: (tool: string) => void
  availableFeatures: Feature[]
}

// use-vendor-store.ts — 供应商数据
interface VendorStore {
  vendors: Vendor[]
  loading: boolean
  fetchVendors: (tool: string) => Promise<void>
  addVendor: (tool: string, vendor: VendorInput) => Promise<void>
  deleteVendor: (id: number) => Promise<void>
  activateVendor: (tool: string, id: number) => Promise<void>
}
```

---

## 7. 错误处理

**Rust 端**：

```rust
#[derive(Debug, thiserror::Error)]
enum AppError {
    #[error("数据库错误: {0}")]
    Database(#[from] rusqlite::Error),
    #[error("文件操作失败: {0}")]
    FileSystem(#[from] std::io::Error),
    #[error("配置解析失败: {0}")]
    ConfigParse(String),
    #[error("工具未安装: {0}")]
    ToolNotInstalled(String),
    #[error("命令执行失败: {0}")]
    ShellCommand(String),
}
```

**前端**：统一通过 `useTauriCommand` Hook 处理，配合 shadcn/ui Toast 通知展示。

---

## 8. 安全性

| 方面 | 措施 |
|------|------|
| API Token 存储 | SQLite 存储，OS 级目录权限保护 |
| Shell 命令 | 仅允许预定义命令白名单 |
| Tauri Capabilities | 最小权限原则（fs、shell、sql） |
| CSP 策略 | 配置适当的 Content Security Policy |

---

## 9. 第一版迁移范围

| 优先级 | 功能 | 版本 |
|--------|------|------|
| P0 | 项目基础架构搭建 | v0.1.0 |
| P0 | 动态侧边栏 + 工具选择器 | v0.1.0 |
| P0 | Claude Code 供应商管理 | v0.1.0 |
| P0 | Codex 供应商管理 | v0.1.0 |
| P1 | Claude Code 工具管理 | v0.1.0 |
| P1 | Codex 工具管理 | v0.1.0 |
| P1 | MCP 服务器管理 | v0.1.0 |
| P1 | CLAUDE.md 编辑 | v0.1.0 |
| P2 | 项目配置管理 | v0.1.0 |
| P2 | 设置页（主题 + 语言） | v0.1.0 |
| P3 | MCP 市场 | v0.2.0（后续迭代） |

---

## 10. 技术栈总览

| 层级 | 技术 |
|------|------|
| 框架 | Tauri v2 |
| 前端 | React 19 + TypeScript |
| 构建工具 | Vite |
| UI 组件 | shadcn/ui |
| 样式 | Tailwind CSS |
| 路由 | TanStack Router |
| 状态管理 | Zustand |
| 图标 | FontAwesome |
| 国际化 | i18next + react-i18next |
| 后端 | Rust |
| 数据库 | SQLite (rusqlite) |
| 配置解析 | serde_json + toml |
| 错误处理 | thiserror |
